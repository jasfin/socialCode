<li id="comment-<%=comment._id%>">
    <p>
        <% if(locals.userFound && locals.userFound.id==comment.user.id) {%>
            <small>
                <a class = "delete-comment-button" href="/comments/delete/<%=comment.id%>">XX</a>
            </small>
        <% }%>
          <%= comment.content %>
          <br>
          <small>
             <%= comment.user.name %>
          </small>
     </p>
  </li>  